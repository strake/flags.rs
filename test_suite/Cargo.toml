[project]
name = "test_suite"
version = "0.0.0"

[features]
unstable = ["compiletest_rs"]

[dependencies]
flags = { path = "../" }
compiletest_rs = { version = "*", optional = true }
serde = "1.0"
serde_derive = "1.0"
serde_json = "1.0"
